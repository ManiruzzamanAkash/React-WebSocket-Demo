import React from 'react';
import Layout from '../components/Layout';
const Home = (props) => {
    return (
        <Layout>
            <h3>Home Page</h3>
        </Layout>
    );
}

export default Home;