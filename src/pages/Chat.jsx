import React from 'react';
import Layout from '../components/Layout';
const Chat = (props) => {
    return (
        <Layout>
            <h3>Chat Page</h3>
        </Layout>
    );
}

export default Chat;